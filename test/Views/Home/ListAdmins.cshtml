@model List<YourNamespace.Models.Employee>
<form method="get" action="/Employees/ListAdmins">
    <input type="text" name="searchQuery" placeholder="Пошук за ім'ям або прізвищем" />
    <button type="submit">Пошук</button>
</form>
<table class="table">
    <thead>
        <tr>
            <th>Ім'я</th>
            <th>Прізвище</th>
            <th>Роль</th>
            <th>Дії</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var admin in Model)
        {
            <tr>
                <td>@admin.FirstName</td>
                <td>@admin.LastName</td>
                <td>@admin.Role</td>
                <td>
                    <a href="@Url.Action("DeleteEmployee", "Employees", new { id = admin.Id })" class="btn btn-danger">Видалити</a>
                </td>
            </tr>
        }
    </tbody>
</table>
<a asp-controller="Home" asp-action="Admin" class="btn btn-secondary">Назад</a>